spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kunde
    username: postgres
    password: 321

   # url: jdbc:postgresql://containers-us-west-61.railway.app:5472/railway
   # username: postgres
   # password: mS2srNP3hvlFtGCQXvOQ

  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
      jdbc:
        lob:
          non_contextual_creation: true

    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    user:
      name: root
      password: root

  servlet:
    multipart:
      max-file-size: 150MB
      max-request-size: 150MB

server:
  error:
    include-message: always
port: 8080
